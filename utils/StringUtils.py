class StringUtils:

    def get_tupple_result(self, x, y, results):
        coordinate_x_y = '(' + x + ',' + y + ')'
        results_test = results
        tuples = (coordinate_x_y, results_test)
        return tuples
